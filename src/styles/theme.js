import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#DFE1DF',
      main: '#BFCCBF',
      dark: '#ACB8AC',
      contrastText: '#000000'
    }
  }
});

export default theme;